# calculates the triangular sequence
n=10 #define the circulation to 10 times
for i in range(1,n+1): # 10 circulations
    triangle_numbers = i*(i+1)/2 #calculate triangular sequence
    print(triangle_numbers) #print the result